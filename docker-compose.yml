services:
  authhub:
    build:
      context: .
      dockerfile: services/authhub/Dockerfile
      args:
        CMD_PATH: ./services/authhub/cmd
    container_name: eventflow-authhub
    ports:
      - "${HTTP_PORT}:8080"
    env_file:
      - .env
    